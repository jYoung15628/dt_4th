function multifly(a, b) {
    const result = a * b;
    return result;
}

console.log(multifly(3, 7));
console.log(multifly(2, 2));

function square(a) {
    const result = a * a;
    // return result;
    // console.log(`${result}`); // 문자랑 원하는 변수를 같이 사용할 때 `` 백틱 감싸서 사용 ! 
    console.log(result);

}

// console.log(square(4));
square(11);
square(5);
